% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/30-mortfit.R
\name{mort_fit}
\alias{mort_fit}
\title{Unified front door for all supported model types}
\usage{
mort_fit(
  model = c("baseline", "st", "stp", "mtp", "gph"),
  formula,
  data = NULL,
  data_train = NULL,
  data_test = NULL,
  ...
)
}
\arguments{
\item{model}{One of \code{"baseline"}, \code{"st"}, \code{"stp"}, \code{"mtp"}, \code{"gph"}.}

\item{formula}{A survival formula.}

\item{data, data_train, data_test}{See \code{baseline_fit()}.}

\item{...}{Passed through to the respective \code{*_fit()} function.}
}
\value{
An object of class \code{BaselineModel}, \code{STModel}, \code{STPModel},
\code{MTPModel}, or \code{GPHModel}.
}
\description{
Unified front door for all supported model types
}
\examples{
set.seed(1)
df <- data.frame(
  birthage     = runif(80, 80, 90),
  censoredage  = runif(80, 90, 105),
  DthIndicator = rbinom(80, 1, 0.4)
)
\donttest{
if (requireNamespace("GenSA", quietly = TRUE)) {
  m0 <- mort_fit("baseline",
    formula = Surv(birthage, censoredage, DthIndicator) ~ 1,
    data = df, AC = "Gompertz", patience = 25)
  m0
}
}
}
