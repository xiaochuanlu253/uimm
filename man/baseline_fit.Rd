% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/30-mortfit.R
\name{baseline_fit}
\alias{baseline_fit}
\title{Fit a baseline age-only parametric model (PAC)}
\usage{
baseline_fit(
  formula,
  data = NULL,
  data_train = NULL,
  data_test = NULL,
  AC = "Beard",
  patience = 500,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{formula}{\code{Surv(birthage, censoredage, DthIndicator) ~ 1}}

\item{data, data_train, data_test}{Training/test data. If \code{data} is a
data frame it is treated as training data.}

\item{AC}{AC family (\code{"Beard"}, \code{"Gompertz"}, …) or a \code{PAC} object.}

\item{patience, tol, verbose}{Passed to \code{fit()}.}
}
\value{
A \code{BaselineModel} object.
}
\description{
Fit a baseline age-only parametric model (PAC)
}
\examples{
set.seed(1)
df <- data.frame(
  birthage     = runif(100, 80, 90),
  censoredage  = runif(100, 90, 105),
  DthIndicator = rbinom(100, 1, 0.4)
)
\donttest{
if (requireNamespace("GenSA", quietly = TRUE)) {
  b0 <- baseline_fit(Surv(birthage, censoredage, DthIndicator) ~ 1,
                     data = df, AC = "Gompertz", patience = 30)
  b0
}
}
}
